# POKER SWIPE V17.4 — HARD BOOT FIX

Исправление мобильного старта.

Главное:
- редактирование имени больше не является критической зависимостью старта;
- обработчик карандаша использует безопасный wrapper;
- если редактор сломается, PLAY / МОИ / YOU продолжают работать;
- глобальные JS-ошибки после успешного запуска больше не заменяют весь интерфейс на STARTUP ERROR;
- GTO Brain остаётся встроенным в index.html;
- добавлены cache-control meta hints;
- бейдж новой версии: `V17.4 · HARD BOOT FIX`.

Для GitHub Pages достаточно заменить `index.html`.
