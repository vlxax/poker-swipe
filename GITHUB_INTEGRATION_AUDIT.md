# PokerSwipe Exploit Engine — GitHub Integration Audit

This is a reduced source package from Sprint 4.13. Generated `*-results.json`, execution snapshots, old sprint reports, and historical sprint markdown were intentionally removed.

## Cursor task

1. Work only in the integration branch. Do not merge to `main`.
2. Do not redesign UI or rewrite architecture before testing.
3. Run `npm run test:all` from this directory.
4. Record the actual Node/npm versions and every failing suite.
5. Never mark an unexecuted test PASS.
6. Fix only reproducible production-code blockers. Re-run the smallest affected suite after each fix, then `npm run test:all` again.
7. Inspect runtime integration points in `exploit-app-integration.js`, `exploit-ui-adapter.js`, `exploit-session-controller.js`, and `exploit-poker-brain.js`.
8. Do not integrate into the production UI yet. First produce `INTEGRATION_AUDIT_REPORT.md` with: executed suites, pass/fail counts, stack traces/root causes, regression status, determinism status, blockers, and `CAN INTEGRATE INTO MAIN APP: YES/NO`.

Do not restore deleted generated result/report files merely to satisfy a file-existence gate. If a runner incorrectly requires stale generated artifacts before executing tests, repair the runner so results are generated by the current run.
