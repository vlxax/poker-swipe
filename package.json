{
  "name": "pokerswipe-polyana-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node tests/polyana_regression.js && node tests/polyana_map.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "jsdom": "^26.1.0",
    "wrangler": "^4.31.0"
  }
}
