# «НУ ЧТО ОПЯТЬ НЕ ТАК?»
## Визуальная концепция: покерное расследование / CSI

Главная задача режима — научить человека **видеть причинность**, а не угадывать улицу.

# ОСНОВНОЙ ЭКРАН — CASE TIMELINE

Верх:
**ДЕЛО #024**
`K♣ 8♦ 3♣ 4♥ 4♠`

Под ним вертикальная линия.

### PREFLOP
`BTN` 2.2 BB  
`BB` call

│

### FLOP
`BB` check  
`BTN` **75%**  
`BB` call

│

### TURN
`BB` check  
`BTN` **125%**  
`BB` call

│

### RIVER
`BB` check  
`BTN` **125%**

Каждое действие — отдельный маленький node, а не вся улица одной кнопкой.

---

# ЖЕСТ «НАЙТИ МЕСТО»

По timeline двигается маленькая круглая **лупа**.

Реализация:
- обычный `position:absolute`;
- pointermove;
- ближайший action-node получает `scale(1.04)`.

Тап по node:
- рамка розовая;
- появляется маленький tag `ПОДОЗРЕВАЮ`.

Внизу:
**Я УВЕРЕН →**

После фиксации нельзя перетыкивать.

---

# REVEAL — НЕ ТЕКСТ, А КАРТА ПРИЧИННОСТИ

Timeline перекрашивается:

PREFLOP `зелёный`
FLOP `жёлтый · первый сигнал`
TURN `красный · главная ошибка`
RIVER `серый · следствие`

Между FLOP и TURN появляется стрелка:
`крупный pot → меньше SPR → дороже river`

Это можно сделать CSS-line + labels.

---

# «ПОЧЕМУ?»

Не список из четырёх одинаковых кнопок.

Показываем 4 небольших evidence-card:
- `слишком рано поляризовали range`;
- `вэлью слишком тонкое`;
- `оппонент capped`;
- `не хватает блефов`.

Пользователь **кладёт одну карточку в папку дела** внизу.

Папка визуально:
`МОЯ ВЕРСИЯ`

После:
`ТОЧКА ✓`
`ЛОГИКА ✕`

Фриковая Дама:
> **Место преступления нашёл. Мотив пока сочинил.**

---

# «А ТЕПЕРЬ ПОЧИНИ»

Timeline возвращается к проблемному node.

Неправильное действие `BET 125%` визуально приподнимается.

Внизу лежат альтернативы как poker chips:
`CHECK`
`25`
`33`
`75`

Пользователь **перетаскивает новую фишку на место старой**.

После timeline пересчитывается.

### Было
River pot `48 BB`
SPR `0.6`

### Стало
River pot `31 BB`
SPR `1.1`

Главный вывод:
> Меньший flop-size не «экономит деньги ради экономии». Он сохраняет больше пространства на следующих улицах.

---

# CASE «ОШИБКИ НЕТ»

Раз в 8–10 кейсов.

Внизу отдельная зона:
**НИГДЕ. ЛИНИЯ НОРМАЛЬНАЯ.**

Чтобы пользователь не обучался искать преступление автоматически.

---

# REPORT РЕЖИМА

После 5 cases:

### КАК ТЫ РАЗБИРАЕШЬ РУКИ

`НАХОДИШЬ ТОЧКУ      81`
`ПОНИМАЕШЬ ПРИЧИНУ   54`
`УМЕЕШЬ ПОЧИНИТЬ     68`
`ВИНИШЬ РИВЕР ПОЗДНО 42`

Последний показатель:
насколько часто пользователь выбирает дорогую последнюю улицу вместо более ранней причины.

---

# Техническая реализация

- timeline = обычные div;
- вертикальная линия = pseudo-element;
- action nodes = buttons/div;
- лупа = absolute div;
- replacement = Pointer Events;
- pot geometry считается JS;
- никакой canvas не нужен.
